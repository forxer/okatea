
diaryList:
    path:     /agenda/{date}
    defaults: { controller: DiaryController::diaryList, date: '' }
    requirements:
        date: '([0-9]{4}/[0-9]{2})?'

diaryEvent:
    path:     /evenement/{slug}
    defaults: { controller: DiaryController::diaryEvent }
