
galleriesList:
    path:     /galleries
    defaults: { controller: GalleriesController::galleriesList }    

galleriesGallery:
    path:     /gallery/{slug}
    defaults: { controller: GalleriesController::galleriesGallery }
    requirements:
        slug: .+?

galleriesItem:
    path:     /galleries/{slug}
    defaults: { controller: GalleriesController::galleriesItem }
    requirements:
        slug: .+?
