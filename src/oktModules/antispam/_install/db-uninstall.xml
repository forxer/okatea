<?xml version="1.0" encoding="utf-8"?>

<database>

	<action id="drop_mod_spamrule" label="Drop table %s" string="{{PREFIX}}mod_spamrule">
		<test eq="eq" value="{{PREFIX}}mod_spamrule" label="Table %s doesn't exists" type="wrn"
		string="{{PREFIX}}mod_spamrule">SHOW TABLES LIKE '{{PREFIX}}mod_spamrule'</test>
		DROP TABLE IF EXISTS `{{PREFIX}}mod_spamrule`
	</action>

</database>