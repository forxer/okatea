<?xml version="1.0" encoding="utf-8"?>

<database>

	<action id="truncate_mod_menus" label="Truncate table %s" string="{{PREFIX}}mod_menus">
		<test eq="eq" value="{{PREFIX}}mod_menus" label="Table %s doesn't exists" type="wrn"
		string="{{PREFIX}}mod_menus">SHOW TABLES LIKE '{{PREFIX}}mod_menus'</test>
		TRUNCATE TABLE `{{PREFIX}}mod_menus`
	</action>

	<action id="truncate_mod_menus_items" label="Truncate table %s" string="{{PREFIX}}mod_menus_items">
		<test eq="eq" value="{{PREFIX}}mod_menus_items" label="Table %s doesn't exists" type="wrn"
		string="{{PREFIX}}mod_menus_items">SHOW TABLES LIKE '{{PREFIX}}mod_menus_items'</test>
		TRUNCATE TABLE `{{PREFIX}}mod_menus_items`
	</action>

	<action id="truncate_mod_menus_items_locales" label="Truncate table %s" string="{{PREFIX}}mod_menus_items_locales">
		<test eq="eq" value="{{PREFIX}}mod_menus_items_locales" label="Table %s doesn't exists" type="wrn"
		string="{{PREFIX}}mod_menus_items_locales">SHOW TABLES LIKE '{{PREFIX}}mod_menus_items_locales'</test>
		TRUNCATE TABLE `{{PREFIX}}mod_menus_items_locales`
	</action>

</database>